commands:
  test: >
    npm ci --prefer-offline &&
    npx playwright install chromium &&
    CALIBRATE_E2E_CAPTURE_SCREENSHOTS=1
    npx nx run web-e2e:e2e-seeded-catalog --outputStyle=static

auto_fix:
  test: 0

no_ci: true
